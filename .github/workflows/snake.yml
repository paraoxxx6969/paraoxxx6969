name: Test Actions

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test
        run: echo "GitHub Actions is working!"
